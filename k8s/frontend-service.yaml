apiVersion: v1
kind: Service
metadata:
  name: ml-frontend
spec:
  selector:
    app: ml-frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
      nodePort: 30173
  type: NodePort
